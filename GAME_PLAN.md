# GAME_PLAN.md

게임 제목: Ready To Destroy
장르: 슈팅/회피 (탑다운 슈팅)
한 문장 규칙: 좌우로 이동하며 위에서 내려오는 적을 총알로 쏴서 제거하고, 적과 충돌하면 목숨을 잃는다.
플레이어 목표: 목숨을 모두 잃기 전까지 최대한 오래 생존하며 많은 적을 제거해 높은 점수를 획득한다.

조작법:
- 방향키 ←/→/↑/↓ (또는 W/A/S/D): 상하좌우 및 대각선 자유 이동 (대각선 이동 시 속도 정규화 적용)
- Space: 총알 발사 (수동 발사, 속사 활성 중에는 홀드 자동 발사)
- Enter 또는 R: 이름 입력 확인 후 게임 시작 / 재시작 (이름 입력창 안에서는 Enter만 제출, R은 이름 문자로 입력)
- M: 효과음 켜기 / 끄기

점수 계산:
- 총알로 적을 제거할 때만 점수 획득 (적 1마리 제거당 50점, 생존 시간 점수 없음)
- 플레이어와 충돌해서 사라진 적은 점수에 반영하지 않음 (목숨만 감소)
- 게임 화면에 실시간으로 현재 점수 표시
- 게임오버 때 이름·최종 점수를 로컬 순위에 저장하고 상위 10위 및 이번 판 순위를 표시 (동점은 먼저 달성한 기록 우선)

게임 종료 조건:
- 목숨(체력) 제도, 시작 시 목숨 3개
- 플레이어와 적이 충돌할 때마다 목숨 1개 감소
- 목숨이 0이 되면 즉시 게임 종료

난이도 상승:
- 시간이 지날수록 적의 하강 속도 증가
- 시간이 지날수록 적의 생성 빈도(스폰 간격) 증가

재시작 방식:
- 게임 오버 화면에서 새 이름을 입력한 뒤 "재출격" 버튼 클릭 (또는 입력창에서 Enter)
- 매 판 이름을 새로 입력해야 하며 공백 이름은 허용하지 않음. 최대 12자, 한글·영문·숫자·특수기호 모두 허용
- 재시작 시 애니메이션 루프, 타이머, 적/총알 배열, 점수, 목숨, 입력 상태를 모두 초기화
- 빠른 연타(더블 클릭/중복 키 입력) 방지 가드 적용

화면 구성:
1. 시작 화면: 전술 콘솔형 패널 안에 게임 제목, 임무 규칙, 키캡 형태 조작법 안내, 전투강화 모듈 3종 아이콘·효과 설명, 조종사 이름 입력, 시작 버튼
2. 플레이 화면: 플레이 영역, 다층 벡터 플레이어(델타윙 함선), 적(드론/스카웃/크루저), 잔광 총알, 6자리 점수, 함선 코어형 남은 목숨, 전투 모듈 HUD
3. 게임 오버 화면: 위험색 전술 패널, 조종사 이름, 6자리 최종 점수, 로컬 TOP 10·현재 순위, 다음 조종사 이름 입력, 재출격 버튼

시각 테마 (스타트렉 패러디, 시각적 요소만 차용):
- 배경: 어두운 우주 공간에 서로 다른 속도의 원·근거리 별층, 희미한 성운·전술 격자·가장자리 비네팅을 CSS만으로 구현 (이미지 리소스 불필요, 저작권 이슈 없음)
- 색상: 네이비/블랙 배경 + 시안(cyan)·블루 계열 네온 강조색 (스타트렉 LCARS를 그대로 베끼지 않고, 미래적인 느낌만 참고)
- 폰트: 시스템에서 사용 가능한 sci-fi/모노스페이스 계열 폰트(예: 'Consolas', 'Courier New' 등 웹 세이프 폰트) 사용, 실제 스타트렉 로고 폰트나 이미지는 사용하지 않음
- 기존 작품의 로고, 함선 디자인, 사운드, 대사 등은 사용하지 않음 (시각적 색감/분위기만 참고)
- 효과음은 외부 음원 없이 Web Audio API로 합성한 독자적인 발사·폭발·피격·아이템·게임 상태 사운드만 사용
- 함선은 원본 다층 SVG 선체·장갑·콕핏·에너지 코어로 구성하고, HUD는 각진 전술 패널과 숫자·형태 아이콘을 사용해 단색 기본 도형 느낌을 줄임

필수 기능:
- 시작 화면 (제목/규칙/조작법/시작 버튼)
- 좌우 이동 (←/→, A/D)
- Space로 총알 발사
- 적 생성 및 하강 이동 (드론/스카웃/크루저 3티어, 가중치 랜덤 스폰, 티어별 확률로 하단 역공)
- 충돌 판정 (총알-적 → hp 감소 후 hp 0일 때 제거, 플레이어-적 → 목숨 감소)
- 점수 계산 (적 제거 수 기반, 티어 무관 1마리당 50점)
- 목숨 표시 및 0일 때 게임 종료
- 게임 종료 화면 및 최종 점수 표시
- 판마다 필수 이름 입력(최대 12자, 문자 종류 제한 없음) 및 브라우저 로컬 점수 순위
- 재시작 기능 (중복 실행 방지 포함)
- 시간 경과에 따른 난이도 상승 (속도 + 스폰 빈도)
- Web Audio API 합성 효과음 및 음소거 버튼/M 단축키

적 티어 상세:
- 드론: 기존 다이아몬드 모양, 빨강, 32x32, 기본 속도, 1타 처치, 하단 통과 시 후방 기습(아래 참고)
- 스카웃: 역삼각형, 주황, 30x30, 드론보다 빠른 속도 + 좌우 드리프트(사인파), 1타 처치
- 크루저: 육각형, 보라, 44x44, 드론보다 느린 속도, 2타 처치 (hp=2)
- 스폰 시 가중치 랜덤으로 티어 결정 (드론 > 스카웃 > 크루저 순으로 등장 빈도)

하단 역공 (신규 메커니즘):
- 적이 화면 하단(PLAY_HEIGHT)을 통과하면 티어별 확률로 랜덤 x 위치에 재배치한 뒤 위쪽으로 이동하는 상태로 전환
- 역공 확률: 드론 100%, 스카웃 20%, 크루저 40%
- 재진입 속도는 원래 하강 속도와 동일 (별도 가속 없음)
- 드론은 예고 없이 즉시 등장
- 스카웃과 크루저는 재진입 위치의 하단 테두리에 0.2초 동안 티어색이 섞인 빨간빛과 이중 상승 화살표를 두 번 점멸한 뒤 등장
- 스카웃은 역공 시작 시 함선을 180도 회전해 날카로운 기수가 위쪽 진행 방향을 향하며, 좌우 드리프트 뱅킹은 그대로 유지
- 화면 위로 완전히 벗어나면 그때 최종 소멸 (역습은 1회만, 무한 반복 아님)
- 충돌 판정(총알-적, 플레이어-적)과 처치 점수(50점)는 기존 로직과 동일하게 적용
- 확률 판정에 실패한 스카웃과 크루저는 기존처럼 하단 통과 시 소멸
- 목적: 하단까지 도달한 적이 다시 위협할 수 있게 하되, 빠른 스카웃과 단단한 크루저는 확률 및 예고 표시로 대응 가능성을 보장

선택 기능 (우선순위 낮음, 요청 시 추후 고려):
- 없음 (위 적 티어 및 하단 역공은 필수 기능으로 승격되어 구현)

격침/충돌 폭발 이펙트 (신규):
- 처리 방식: 별도 게임 상태 배열이나 타이머 없이, 생성 시 CSS 애니메이션을 재생하고 `animationend`에서 스스로 `remove()`하는 발사 후 잊기(fire-and-forget) 방식으로 구현 (재시작 시 `playArea.innerHTML = ""`로 잔여 이펙트도 함께 정리됨)
- 성능 제약 (중요): 애니메이션 중인 요소는 각각 GPU 합성 레이어로 올라간다. 초기 구현은 난전에서 동시 애니메이션 요소가 190개까지 치솟아 GPU가 리셋되면서 화면이 순간 하얗게 되는 문제가 있었다. 다음 세 가지로 72개까지 낮춤:
  - 폭발 파편을 개별 요소 8개 → `conic-gradient` 스포크 1개로 통합 (폭발당 요소 9개 → 2개)
  - 피탄 광선을 자식 요소 6개 → 단일 방사형 섬광 1개로 통합 (피탄당 요소 7개 → 1개)
  - 동시 개수 상한: 폭발 10개 / 피탄 8개. 상한 초과 시 신규 이펙트를 생략한다. 격추 피드백이 더 중요하므로 폭발 쪽 상한을 더 크게 둔다
  - 카운터는 `animationend`에서 감소시키는데, 재시작은 `playArea.innerHTML = ""`로 지워 `animationend`가 오지 않으므로 `startGame()`에서 반드시 함께 0으로 리셋할 것
- 총알 렌더링 성능 제약 (중요): TRIDENT ×6과 BOLT가 겹치면 초당 최대 120발·화면 내 130발 이상이 생긴다. 총알마다 DOM과 가상 요소를 만들면 매 프레임 레이아웃/페인트가 폭증하므로, 이동·충돌 상태는 기존 배열로 유지하되 모든 총알의 잔상·외곽광·코어를 단일 투명 캔버스에 3회 배치 렌더링한다. 좌우로 화면을 완전히 벗어난 대각선 총알도 즉시 배열에서 제거한다.
- 트리거 1 - 격침 (총알로 처치): `checkCollisions()`의 `destroyedEnemies` 처리 구간에서, 파괴된 적의 중심 좌표에 티어색(드론 빨강/스카웃 주황/크루저 보라) 코어 폭발·외곽 충격파·8방향 파편을 생성하고 약 340ms 동안 확산되며 사라짐
- 트리거 2 - 충돌 (플레이어와 부딪힘): `checkCollisions()`의 `collidedWithPlayer` 처리 구간에서
  - 충돌한 적 위치에 동일한 티어색 폭발 이펙트 생성 (격침 때와 색 구분 없음)
  - 플레이어 함선에 짧은(150ms) 흰색/빨강 피격 플래시
  - 플레이 영역 전체에 약하고 짧은(200ms 이내) 화면 흔들림
- 크루저가 첫 피격을 버티면 충돌 위치에 6방향 스파크가 표시되고, 장갑 일부와 코어가 손상 상태로 전환

파워업/아이템 (신규):
- 드롭: 총알로 적을 처치할 때만 15% 확률로 그 자리에 캡슐 아이템 낙하 (70px/s로 서서히 낙하, 화면 밖으로 나가면 소멸)
- 획득: 플레이어가 스치면(충돌 판정) 즉시 획득, 캡슐 소멸
- 종류 (색상 + 모양 모두로 구분, 색맹 등 색상만 의존하지 않도록). 화면 표기명은 TRIDENT / SHIELD / BOLT이며, 코드 내부 키는 각각 spread / shield / rapid로 기능을 그대로 서술한다:
  - TRIDENT / 스프레드샷 (파랑, 포세이돈 삼지창 모양 — 세 갈래가 3방향 발사와 대응): 8초간 발사할 때마다 정면 + 좌우 대각선(수평 속도 ±140px/s) 총알 3방향 동시 발사. 활성 중 한 개 더 먹으면 **6방향**(±50/±140/±260px/s)으로 강화, 지속시간은 그때마다 8초로 갱신. 6방향이 최고 단계이며 그 이상 먹어도 방향 수는 늘지 않음(지속시간만 8초로 갱신)
  - SHIELD / 실드 (초록, 방패 모양): 피격 1회당 충전 1개 소모하여 완전히 무효화 (목숨 감소·피격 플래시·화면 흔들림 없음, 충돌한 적은 그대로 파괴됨). 충전형이라 지속시간 없음. 먹을 때마다 충전 +1, **최대 3개까지 중첩** 가능(4개째부터는 먹어도 늘지 않음). 충전 1개 이상일 때 플레이어 주위에 초록색 원형 방어막 링(은은한 펄스 애니메이션)이 표시되고, 충전이 0이 되면 사라짐
  - BOLT / 속사 (노랑, 번개 모양): 발사 쿨다운이 절반으로 감소 (기본 0.1초 → 0.05초)하며, 활성 중에는 Space를 누르고 있는 동안 쿨다운마다 자동 발사. 활성 중 한 개 더 먹으면 남은 지속시간에 **+8초 추가**(즉시 리셋이 아님), 단 총 지속시간은 **최대 16초**로 제한되어 그 이상은 늘지 않음
- 기본 발사에도 쿨다운 0.1초 도입 (스피디한 발사감 유지 + 속사 파워업의 체감 효과를 위함)
- 중복 획득 규칙 요약: 스프레드샷=방향 수 강화(최대 6방향, 지속시간은 매번 8초로 갱신) / 속사=지속시간 가산(최대 16초) / 실드=충전 개수 누적(최대 3개)
- HUD: 활성 파워업만 아이콘(캡슐과 동일한 색+모양)으로 표시, 스프레드샷/속사는 잔여시간 바 함께 표시, 실드는 "×보유개수" 배지로 충전 수 표시
- 재시작 시 아이템 배열, 쿨다운, 활성 파워업(레벨 포함), 실드 충전 상태 모두 초기화

디자인 후보 검토 (목업: `assets/design_mockup.svg` 참고):

플레이어 함선 디자인 후보
- 인터셉터: 기본형 (기존 삼각형 디자인)
- 셔틀형: 나셀 + 콕핏 디테일 추가
- 델타윙: 스웹백 + 테일노치 (채택, `script.js`의 다층 SVG 선체와 `style.css`의 콕핏·패널·엔진 스타일에 반영 완료)

적 함선 티어 후보
- 드론: 기본, 1타 처치 (채택)
- 스카웃: 빠름, 좌우 드리프트 (채택)
- 크루저: 크고 느림, 2타 처치 (채택)

3종 모두 채택되어 위 "적 티어 상세" 스펙과 다층 SVG 디자인으로 구현 완료. `assets/design_mockup.svg`는 초기 실루엣 후보 기록으로 유지함.

구현 순서:
1. 기본 HTML 화면 및 레이아웃
2. 게임 영역(캔버스 또는 div) 표시
3. 시작 화면과 조작법 UI
4. 플레이어 표시
5. 플레이어 좌우 이동 입력 처리
6. 총알 발사 (Space) 및 이동
7. 적 생성 및 하강 이동
8. 총알-적 충돌 판정 및 제거
9. 플레이어-적 충돌 판정 및 목숨 감소
10. 점수 계산 (적 제거 수 기반)
11. 게임 종료 처리
12. 재시작 기능
13. 빠른 이중 재시작 방지
14. 난이도 상승 (속도 + 스폰 빈도)
15. 이름 입력 및 로컬 점수 순위
16. 시각적 개선 및 최종 검증

테스트 항목: (CLAUDE.md의 단계별 테스트 체크리스트를 각 단계마다 적용)
- 시작 화면 요소 노출 여부
- 좌우 이동이 화면 밖으로 나가지 않는지
- Space 발사 및 총알-적 충돌 정상 동작
- 적 생성/하강 및 플레이어 충돌 시 목숨 감소 정상 동작
- 점수가 정확히 계산되고 게임오버 후 멈추는지
- 목숨 0일 때 게임 종료가 정확히 1회만 실행되는지
- 시작·재출격 시 빈 이름과 12자 초과 이름이 차단되고 한글·영문·숫자·특수기호가 안전하게 표시되는지
- 게임오버마다 점수가 1회만 기록되고 TOP 10·현재 순위가 점수 내림차순으로 표시되며 새로 열어도 유지되는지
- 재시작 시 모든 상태(점수, 목숨, 적, 총알, 타이머, 루프)가 초기화되는지
- 재시작 버튼 빠른 연타 시 중복 문제 없는지
- 드론이 하단 통과 시 항상 즉시 역공하고, 스카웃 20%·크루저 40% 확률 판정 및 0.2초 하단 예고 후 역공이 정확한지, 모든 티어가 화면 위로 벗어나야 최종 소멸하며 역공은 1회만 수행하는지
- 격침/충돌 시 티어색 폭발 이펙트가 정확한 위치에 표시되고 스스로 정리되는지, 충돌 시 피격 플래시·화면 흔들림이 함께 동작하는지, 재시작 시 잔여 이펙트가 남지 않는지
- 아이템 드롭 확률·종류 분포가 의도한 범위인지, 낙하·획득·효과 적용(스프레드샷 3방향 발사, 속사 쿨다운 절반 및 Space 홀드 자동 발사, 실드 1회 무효화)이 정확한지, 재획득 시 지속시간이 갱신되는지, 실드가 목숨 감소를 완전히 막는지, 재시작 시 아이템/파워업 상태가 모두 초기화되는지
- 발사·격침·피격·실드·아이템 획득·게임 시작/종료 효과음이 상황에 맞게 재생되는지, 음소거 버튼과 M 단축키가 즉시 반영되는지
- 난전(연사 + 다수 적 + 폭발 연속)에서 동시 애니메이션 요소가 과도하게 쌓이지 않는지, 이펙트 상한이 지켜지는지, 재시작 후 이펙트 카운터가 0으로 돌아오는지
- TRIDENT ×6 + BOLT를 동시에 활성화하고 Space를 10초 이상 홀드해도 프레임 지연이나 공백 화면이 발생하지 않는지
- 콘솔 오류 없는지

제출용 설명:
- 제출용 제목: Ready To Destroy
- 제출용 한 문장 규칙: 좌우로 이동하며 내려오는 적을 총알로 제거하고, 적과 충돌하면 목숨을 잃는 슈팅 게임입니다.
- 제출용 조작법: 방향키/WASD로 상하좌우 및 대각선 이동, Space 발사, Enter/R 시작 및 재시작, M 효과음 켜기/끄기
- 제출용 장르: 슈팅/회피
