# CLAUDE.md

## 프로젝트 개요

이 프로젝트는 **팡팡 아케이드** 제출용 브라우저 미니게임이다.

게임은 HTML, CSS, Vanilla JavaScript로 구현하며, 별도의 설치나 빌드 과정 없이 `index.html`을 직접 열어 실행할 수 있어야 한다.

최종 제출물은 ZIP 파일이며, 압축을 풀었을 때 `index.html`이 최상위에서 바로 보여야 한다.

---

## 과제 진행 원칙

이 프로젝트는 다음 순서로 진행한다.

1. 사용자와 게임 장르 및 핵심 아이디어를 기획한다.
2. 규칙, 조작법, 점수, 종료 조건, 재시작 방식 등을 구체화한다.
3. 논의한 기획 내용을 별도의 Markdown 문서로 정리한다.
4. 완성된 기획 문서를 기준으로 구현을 시작한다.
5. 모든 기능을 한 번에 만들지 않고 기능 하나씩 구현한다.
6. 각 기능을 구현한 뒤 브라우저에서 직접 확인한다.
7. 오류, 이상 동작, 버그가 발생하면 해당 부분만 분석하고 수정한다.
8. 최종적으로 제출 체크리스트를 검증한 뒤 ZIP으로 압축한다.

기획이 완료되기 전에는 임의로 게임 전체 구현을 시작하지 않는다.

---

## 필수 제출 기능

게임은 반드시 다음 조건을 만족해야 한다.

- 게임 시작 전에 간단한 규칙과 조작법이 표시된다.
- 한 판마다 점수를 계산한다.
- 명확한 게임 종료 조건이 있다.
- 게임 종료 후 다시 시작할 수 있다.
- 다시 시작 버튼을 빠르게 두 번 눌러도 정상 작동한다.
- 브라우저에서 `index.html`을 열어 바로 실행된다.
- ZIP 압축을 풀었을 때 `index.html`이 바로 보인다.

---

## 기술 제약

기본적으로 다음 기술만 사용한다.

- HTML
- CSS
- Vanilla JavaScript
- 표준 브라우저 API

명시적인 요청이 없는 한 다음 기술은 사용하지 않는다.

- React
- Vue
- TypeScript
- npm 패키지
- 번들러
- 빌드 도구
- 백엔드 서버
- 데이터베이스
- 로그인 기능

권장 파일 구조:

```text
project-root/
├─ index.html
├─ style.css
├─ script.js
├─ assets/
├─ GAME_PLAN.md
└─ CLAUDE.md
```

프로젝트가 매우 단순한 경우에는 CSS와 JavaScript를 `index.html` 안에 포함해도 된다.

---

## 기획 단계 규칙

게임 기획은 다음 항목을 순서대로 확정한다.

1. 장르
2. 게임 제목
3. 한 문장 규칙
4. 플레이어의 목표
5. 조작법
6. 점수 계산 방식
7. 게임 종료 조건
8. 난이도 상승 방식
9. 재시작 방식
10. 화면 구성
11. 필요한 오브젝트
12. 최소 구현 범위
13. 선택 확장 기능

사용자가 선택하기 쉽도록 한 번에 너무 많은 질문을 하지 않는다.

장르, 조작 방식, 점수 방식처럼 중요한 결정을 먼저 제시하고 선택지를 제공한다.

기획이 끝나면 모든 논의 내용을 `GAME_PLAN.md`에 정리한다.

`GAME_PLAN.md`에는 최소한 다음 내용이 포함되어야 한다.

```text
게임 제목:
장르:
한 문장 규칙:
조작법:
플레이어 목표:
점수 계산:
게임 종료 조건:
난이도 상승:
재시작 방식:
화면 구성:
필수 기능:
선택 기능:
구현 순서:
테스트 항목:
제출용 설명:
```

---

## 구현 단계 규칙

모든 기능을 한 번에 구현하지 않는다.

권장 구현 순서:

1. 기본 HTML 화면
2. 게임 영역 표시
3. 시작 화면과 조작법
4. 플레이어 표시
5. 플레이어 입력
6. 장애물 또는 목표물 생성
7. 이동 처리
8. 충돌 또는 성공 판정
9. 점수 계산
10. 게임 종료
11. 재시작
12. 빠른 이중 재시작 방지
13. 난이도 조절
14. 시각적 개선
15. 최종 검증

각 단계가 끝날 때마다 다음을 수행한다.

- 변경된 파일 확인
- 브라우저에서 실제 실행
- 콘솔 오류 확인
- 기존 기능이 깨지지 않았는지 확인
- 다음 기능으로 넘어가도 되는 상태인지 보고

한 단계에서 문제가 발생하면 다음 단계로 넘어가지 않는다.

---

## 게임 상태 관리

게임은 명확한 상태로 관리한다.

기본 상태:

```text
ready
playing
gameOver
```

필요할 경우:

```text
paused
```

권장 예시:

```js
let gameState = "ready";
```

입력, 이동, 충돌, 점수, 렌더링, 게임 종료 로직은 현재 게임 상태를 확인해야 한다.

여러 개의 불분명한 Boolean 변수 대신 하나의 게임 상태를 우선 사용한다.

---

## 게임 루프

애니메이션이 필요한 게임은 하나의 `requestAnimationFrame` 루프만 사용한다.

```js
function gameLoop(timestamp) {
  if (gameState !== "playing") return;

  const deltaTime = getDeltaTime(timestamp);

  update(deltaTime);
  checkCollisions();
  render();

  animationFrameId = requestAnimationFrame(gameLoop);
}
```

새 게임을 시작할 때 기존 루프가 남아 있지 않도록 한다.

여러 개의 애니메이션 루프가 동시에 실행되면 안 된다.

---

## 재시작 안정성

재시작 기능은 과제의 핵심 검증 항목이다.

재시작 버튼을 빠르게 두 번 이상 눌러도 다음 문제가 발생하면 안 된다.

- 게임 속도가 두 배로 빨라짐
- 장애물이 중복 생성됨
- 점수가 중복 증가함
- 이벤트 리스너가 여러 번 등록됨
- 타이머가 여러 개 실행됨
- 플레이어가 중복 생성됨
- 게임 상태가 꼬임

재시작 전에는 다음 항목을 정리한다.

- 활성 `requestAnimationFrame`
- `setInterval`
- `setTimeout`
- 생성된 오브젝트 배열
- 플레이어 위치와 상태
- 점수
- 시간
- 입력 상태
- 게임 오버 화면
- 시작 및 재시작 버튼 상태

권장 예시:

```js
let animationFrameId = null;
let spawnIntervalId = null;
let isRestarting = false;

function restartGame() {
  if (isRestarting) return;

  isRestarting = true;

  stopGame();
  resetGameState();
  startGame();

  requestAnimationFrame(() => {
    isRestarting = false;
  });
}
```

이벤트 리스너는 초기화 단계에서 한 번만 등록하는 것을 원칙으로 한다.

---

## 입력 처리

조작법은 단순하고 화면에 명확히 표시한다.

예시:

- 방향키 또는 WASD: 이동
- Space: 행동
- Enter 또는 R: 시작 및 재시작
- 마우스 클릭: 목표 선택

방향키나 Space를 사용할 경우 브라우저 스크롤을 방지한다.

```js
const keys = new Set();

window.addEventListener("keydown", (event) => {
  if (
    ["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight", " "]
      .includes(event.key)
  ) {
    event.preventDefault();
  }

  keys.add(event.key);
});

window.addEventListener("keyup", (event) => {
  keys.delete(event.key);
});
```

게임 시작 전이나 게임 종료 후에 키를 눌러도 오류가 발생하면 안 된다.

---

## 점수 규칙

점수는 플레이 중 항상 확인 가능해야 한다.

점수 방식 예시:

- 생존 시간
- 수집한 아이템 수
- 피한 장애물 수
- 맞힌 목표 수
- 완료한 행동 수

게임 종료 화면에는 다음을 표시한다.

- 최종 점수
- 결과 메시지
- 다시 시작 버튼

새 게임이 시작될 때 점수는 정확히 한 번 0으로 초기화한다.

---

## 게임 종료 규칙

게임 종료 조건은 명확하고 예측 가능해야 한다.

예시:

- 장애물과 충돌
- 체력이 0이 됨
- 제한 시간이 끝남
- 목표물을 일정 횟수 놓침
- 적이 특정 지점에 도달함

게임 종료 함수는 한 판에 한 번만 실행한다.

```js
function endGame() {
  if (gameState !== "playing") return;

  gameState = "gameOver";
  stopGame();
  showGameOverScreen();
}
```

게임 종료 후에는 점수나 오브젝트가 계속 변하지 않아야 한다.

---

## 화면 및 UX

게임 시작 화면에는 다음이 있어야 한다.

- 게임 제목
- 한 문장 규칙
- 조작법
- 시작 버튼

게임 화면에는 다음이 있어야 한다.

- 플레이 영역
- 현재 점수
- 필요한 경우 남은 시간 또는 체력

게임 종료 화면에는 다음이 있어야 한다.

- 게임 종료 문구
- 최종 점수
- 다시 시작 버튼

UX 원칙:

- 노트북 화면에서 잘 보여야 한다.
- 플레이어와 장애물은 쉽게 구분되어야 한다.
- 중요한 정보는 색상에만 의존하지 않는다.
- 버튼은 실제 `<button>` 요소를 사용한다.
- 텍스트는 읽기 쉬워야 한다.
- 지나친 깜빡임과 과도한 효과를 피한다.
- 사용자가 플레이 방법을 추측하게 하지 않는다.

사용자 요청이 없다면 화면 문구는 한국어로 작성한다.

---

## 파일과 에셋

모든 파일 경로는 상대경로를 사용한다.

올바른 예:

```html
<link rel="stylesheet" href="./style.css">
<script src="./script.js" defer></script>
```

사용하지 말아야 할 예:

```text
C:\Users\...
D:\project\...
```

이미지, 소리, 폰트가 필요하면 가능하면 `assets` 폴더에 저장한다.

인터넷 연결 없이도 실행되는 구성을 우선한다.

저작권이 불분명한 이미지나 음원을 임의로 사용하지 않는다.

---

## Claude Code 작업 방식

Claude Code는 작업을 시작하기 전에 기존 파일을 먼저 확인한다.

작업 시 다음 원칙을 따른다.

- 현재 파일 구조를 먼저 파악한다.
- 지금 구현할 기능 하나를 명확히 한다.
- 필요한 최소 범위만 수정한다.
- 이미 작동하는 기능을 불필요하게 다시 작성하지 않는다.
- 변경한 파일명을 정확히 보고한다.
- 실제로 확인하지 않은 테스트를 통과했다고 말하지 않는다.
- 오류가 있으면 추측만 하지 말고 원인을 확인한다.
- 사용자가 제공한 스크린샷과 오류 메시지를 우선 분석한다.
- 기획 문서와 다른 구현을 임의로 추가하지 않는다.
- 추가 아이디어는 구현하지 말고 선택 기능으로 제안한다.

응답은 길게 이론을 설명하기보다 다음을 중심으로 한다.

1. 지금 구현하거나 수정할 내용
2. 변경한 파일
3. 직접 확인할 방법
4. 남은 문제 또는 다음 단계

---

## 단계별 테스트 체크리스트

### 시작 화면

- [ ] `index.html`이 직접 열린다.
- [ ] 게임 제목이 보인다.
- [ ] 한 문장 규칙이 보인다.
- [ ] 조작법이 보인다.
- [ ] 시작 버튼이 작동한다.

### 기본 플레이

- [ ] 플레이어 조작이 정상이다.
- [ ] 플레이어가 게임 영역 밖으로 나가지 않는다.
- [ ] 장애물 또는 목표물이 정상 생성된다.
- [ ] 점수가 정상 계산된다.
- [ ] 종료 조건이 정상 작동한다.
- [ ] 콘솔 오류가 없다.

### 게임 종료

- [ ] 종료 함수가 한 번만 실행된다.
- [ ] 최종 점수가 표시된다.
- [ ] 종료 후 점수가 더 변하지 않는다.
- [ ] 종료 후 오브젝트가 계속 생성되지 않는다.
- [ ] 다시 시작 버튼이 표시된다.

### 재시작

- [ ] 한 번 눌렀을 때 정상 재시작된다.
- [ ] 여러 판을 반복해도 정상이다.
- [ ] 빠르게 두 번 눌러도 정상이다.
- [ ] 게임 속도가 중복 증가하지 않는다.
- [ ] 장애물이 중복 생성되지 않는다.
- [ ] 점수가 정확히 초기화된다.
- [ ] 기존 타이머가 남지 않는다.
- [ ] 이벤트 리스너가 중복 등록되지 않는다.

### 제출

- [ ] `index.html`이 포함되어 있다.
- [ ] 필요한 CSS, JS, assets 파일이 포함되어 있다.
- [ ] 누락된 파일 오류가 없다.
- [ ] ZIP을 풀었을 때 `index.html`이 바로 보인다.
- [ ] 게임명이 정해져 있다.
- [ ] 한 문장 규칙이 준비되어 있다.
- [ ] 조작법이 준비되어 있다.
- [ ] 장르가 준비되어 있다.
- [ ] 제출 상태를 `Done`으로 바꿀 준비가 되어 있다.

---

## 현재 기획 상태

게임 기획이 확정되면 아래 항목을 `GAME_PLAN.md`에 작성한다.

```text
게임 제목:
장르:
한 문장 규칙:
플레이어 목표:
조작법:
점수 계산:
게임 종료 조건:
난이도 상승:
재시작 방식:
화면 구성:
필수 기능:
선택 기능:
구현 순서:
테스트 항목:
제출용 제목:
제출용 한 문장 규칙:
제출용 조작법:
제출용 장르:
```
